{
  "name": "<%= projectName %>",
  "version": "1.0.0",
  "description": "Generated Node.js project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "<% if (useTests) { %>jest<% } else { %>echo \"No tests specified\" && exit 0<% } %>"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",<% if (useSwagger) { %>
    "swagger-ui-express": "^4.6.3",
    "swagger-jsdoc": "^6.2.8",<% } %>
    <% if (dbType === 'mongo') { %>
    "mongoose": "^7.2.2"<% } else { %>
    "sequelize": "^6.32.1",
    "mysql2": "^3.3.5"<% } %>
  },
  "devDependencies": {
    <% if (useTests) { %>"jest": "^29.7.0"<% } %>
  }
}